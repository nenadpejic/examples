{
  "name": "bootstrap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "sass scss:assets/css",
    "watch": "nodemon -e html,scss -x \"npm run css\"",
    "server": "serve --listen 3000",
    "start": "npm-run-all --parallel watch server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.14",
    "npm-run-all": "^4.1.5",
    "sass": "^1.43.3",
    "serve": "^12.0.1"
  }
}
